import React from "react";

const NotFoundPage = () => {
  return (
    <div>
      <h1 className="text-lg">Not Found (custom error handler)</h1>
      <span className="text-sm">(implementation in progress...)</span>
    </div>
  );
};

export default NotFoundPage;